import { Theme } from "@emotion/react";

const theme: Theme = {
  colors: {
    primary: "#30bced",
    secondary: "#303036",
    white: "#fffaff",
    standout: "#fc5130",
    black: "#050401",
  },
};

export default theme;
